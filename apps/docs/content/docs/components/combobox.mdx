---
title: ComboBox
---

import { ComboBox, ComboBoxEmptyExample, Example, ListItem, Text } from '../../../mdx-components';

A combo box combines a text input with a listbox, allowing users to filter a list of options to items matching a query.

<Example>
  <ComboBox>
    <ListItem id="one">One</ListItem>
    <ListItem id="two">Two</ListItem>
    <ListItem id="three">Three</ListItem>
    <ListItem id="four">Four</ListItem>
    <ListItem id="five">Five</ListItem>
    <ListItem id="six">Six</ListItem>
    <ListItem id="seven">Seven</ListItem>
    <ListItem id="eight">Eight</ListItem>
    <ListItem id="nine">Nine</ListItem>
    <ListItem id="ten">Ten</ListItem>
  </ComboBox>

  ```jsx
  <ComboBox>
    <ListItem id="one">One</ListItem>
    <ListItem id="two">Two</ListItem>
    <ListItem id="three">Three</ListItem>
    <ListItem id="four">Four</ListItem>
    <ListItem id="five">Five</ListItem>
  </ComboBox>
  ```
</Example>

## Empty state

When filtering returns no matches, the combo box displays a helpful empty state. Use
`renderEmptyState` to replace it with custom content.

<Example>
  <ComboBoxEmptyExample />

  ```jsx
  <ComboBox
    items={['One', 'Two', 'Three']}
    renderEmptyState={() => <Text color="muted">Nothing matched your search.</Text>}
  />
  ```
</Example>

## Component props

<PropertiesTable>
  <ResponseField name="inputValue" type="string">Controlled input value</ResponseField>
  <ResponseField name="defaultInputValue" type="string">Default input value</ResponseField>
  <ResponseField name="selectedKey" type="string">Controlled selected key</ResponseField>
  <ResponseField name="defaultSelectedKey" type="string">Default selected key</ResponseField>
  <ResponseField name="isDisabled" type="boolean">Disables the combobox</ResponseField>
  <ResponseField name="onInputChange" type="(value: string) => void">Called when input changes</ResponseField>
  <ResponseField name="onSelectionChange" type="(key: string) => void">Called when selection changes</ResponseField>
  <ResponseField name="renderEmptyState" type="Function">Replaces the default empty-state message</ResponseField>
</PropertiesTable>
