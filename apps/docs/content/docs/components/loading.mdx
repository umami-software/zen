---
title: Loading
---

import { Column, Dots, Example, Label, Row, Spinner } from '../../../mdx-components';

An element that indicates content is loading.

## Spinner

<Example>
  <Spinner />

  ```jsx
  <Spinner />
  ```
</Example>

## Dots

<Example>
  <Dots />

  ```jsx
  <Dots />
  ```
</Example>

## Sizes

<Example>
  <Row justifyContent="space-evenly" width="100%">
    <Column alignItems="center" gap>
      <Label>sm</Label>
      <Spinner size="sm" />
      <Label>md</Label>
      <Spinner size="md" />
      <Label>lg</Label>
      <Spinner size="lg" />
    </Column>
    <Column alignItems="center" gap>
      <Label>sm</Label>
      <Dots size="sm" />
      <Label>md</Label>
      <Dots size="md" />
      <Label>lg</Label>
      <Dots size="lg" />
    </Column>
  </Row>

  ```jsx
  <Spinner size="sm" />
  <Spinner size="md" />
  <Spinner size="lg" />
  ```
</Example>

## Component props

<PropertiesTable>
  <ResponseField name="size" type="LoadingSize">"sm" | "md" | "lg"</ResponseField>
  <ResponseField name="icon" type="LoadingIcon">"dots" | "spinner" (default: "spinner")</ResponseField>
  <ResponseField name="placement" type="LoadingPlacement">"absolute" | "center" | "inline" (default: "inline")</ResponseField>
</PropertiesTable>
