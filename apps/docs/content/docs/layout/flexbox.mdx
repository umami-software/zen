---
title: Flexbox
---

import { Column, Example, ExampleBox, Flexbox, Label } from '../../../mdx-components';

A flexible layout component using CSS Flexbox for one-dimensional arrangements. Provides fine-grained control over alignment, distribution, and wrapping. For common cases, prefer the simpler Row and Column components.

<Example gap="8">
  <Column alignItems="center">
    <Label>Row</Label>
    <Flexbox direction="row" gap="3">
      <ExampleBox />
      <ExampleBox />
      <ExampleBox />
    </Flexbox>
  </Column>
  <Column>
    <Label>Column</Label>
    <Flexbox direction="column" gap="3">
      <ExampleBox />
      <ExampleBox />
      <ExampleBox />
    </Flexbox>
  </Column>

  ```jsx
  <Column alignItems="center">
    <Label>Row</Label>
    <Flexbox direction="row" gap="3">
      <ExampleBox />
      <ExampleBox />
      <ExampleBox />
    </Flexbox>
  </Column>
  <Column>
    <Label>Column</Label>
    <Flexbox direction="column" gap="3">
      <ExampleBox />
      <ExampleBox />
      <ExampleBox />
    </Flexbox>
  </Column>
  ```
</Example>

There are convenience components `Row` and `Column` which render a `Flexbox` with the direction pre-selected. You should opt to use them
by default as they provide more semantic meaning and are easier to read.

## Component props

A Flexbox renders a [Box](/docs/layout/box) as a child, so it also accepts all Box props.

<PropertiesTable>
  <ResponseField name="display" type="Responsive<FlexDisplay>">"flex" | "inline-flex" | "none"</ResponseField>
  <ResponseField name="direction" type="Responsive<FlexDirection>">"row" | "column" | "row-reverse" | "column-reverse"</ResponseField>
  <ResponseField name="wrap" type="Responsive<FlexWrap>">"wrap" | "nowrap" | "wrap-reverse"</ResponseField>
  <ResponseField name="justifyContent" type="Responsive<JustifyContent>">"center" | "start" | "end" | "space-between" | "space-around" | ...</ResponseField>
  <ResponseField name="justifyItems" type="Responsive<JustifyItems>">"center" | "start" | "end" | "stretch" | "baseline" | ...</ResponseField>
  <ResponseField name="alignContent" type="Responsive<AlignContent>">"center" | "start" | "end" | "space-between" | "stretch" | ...</ResponseField>
  <ResponseField name="alignItems" type="Responsive<AlignItems>">"center" | "start" | "end" | "stretch" | "baseline" | ...</ResponseField>
  <ResponseField name="gap" type="Responsive<Gap>">true | Spacing scale</ResponseField>
  <ResponseField name="gapX" type="Responsive<Gap>">true | Spacing scale</ResponseField>
  <ResponseField name="gapY" type="Responsive<Gap>">true | Spacing scale</ResponseField>
  <ResponseField name="as" type="string">HTML element to render</ResponseField>
  <ResponseField name="render" type="RenderProp">Render prop for polymorphism</ResponseField>
</PropertiesTable>
